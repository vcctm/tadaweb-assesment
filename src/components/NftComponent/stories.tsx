import { Story, Meta } from "@storybook/react";
import NftComponent from '.'

export default {
  title: 'NftComponent',
  component: NftComponent
} as Meta

export const Default: Story = () => <NftComponent />
