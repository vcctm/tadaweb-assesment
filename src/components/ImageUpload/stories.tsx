import { Story, Meta } from "@storybook/react";
import ImageUpload from '.'

export default {
  title: 'ImageUpload',
  component: ImageUpload
} as Meta

export const Default: Story = () => <ImageUpload />
