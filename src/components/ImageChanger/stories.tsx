import { Story, Meta } from "@storybook/react";
import ImageChanger from '.'

export default {
  title: 'ImageChanger',
  component: ImageChanger
} as Meta

export const Default: Story = () => <ImageChanger />
